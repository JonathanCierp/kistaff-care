// app global css
.q-tab-panel {
  padding: 0px;
}

.list-item {
  border-bottom: 1px solid rgba(0,0,0,0.12);
}

.content-wrapper {
  display: flex;
  flex-direction: column;
  height:calc(100vh - 50px);
}

.main-content {
  height:calc(100vh - 50px);
  overflow-y: scroll;
}

.toolbar {
  height: 50px;
}
